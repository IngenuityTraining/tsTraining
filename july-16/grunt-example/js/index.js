"use strict";
console.error("Welcome !!!");
sum(10, 20);
function sum(x, y) {
    var trace = 'To Task runners';
    if (x < 0) {
        console.warn(trace);
    }
    return x + y;
}
//# sourceMappingURL=index.js.map